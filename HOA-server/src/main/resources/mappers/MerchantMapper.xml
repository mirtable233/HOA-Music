<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.csu.hoaserver.dao.MerchantDao">
    <insert id="save">
        INSERT INTO merchant (user_id, store_name, business_license, address_book_id, contact_name, create_time, update_time)
        VALUES (#{userId}, #{storeName}, #{businessLicense}, #{addressBookId}, #{contactName}, #{createTime}, #{updateTime})
    </insert>
</mapper>