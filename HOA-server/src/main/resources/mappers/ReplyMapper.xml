<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.csu.hoaserver.dao.ReplyDao">

    <insert id="save">
        INSERT INTO reply (comment_id, user_id, reply_user_id, content, create_time, parent_reply_id)
        VALUES (#{commentId}, #{userId}, #{replyUserId}, #{content}, #{createTime}, #{parentReplyId})
    </insert>

    <select id="list" resultType="DO.Reply">
        select id,user_id,reply_user_id,content,create_time,parent_reply_id
        from reply
        where comment_id = #{commentId}
    </select>
</mapper>